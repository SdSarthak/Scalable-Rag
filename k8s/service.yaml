apiVersion: v1
kind: Service
metadata:
  name: rag-system
  labels:
    app: rag-system
spec:
  type: ClusterIP
  selector:
    app: rag-system
  ports:
    - name: http
      port: 80
      targetPort: http

apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-system
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: rag-system
        image: rag-system:latest
        resources:
          limits:
            nvidia.com/gpu: 1  # If using GPU